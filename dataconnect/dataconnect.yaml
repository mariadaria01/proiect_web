specVersion: "v1"
serviceId: "firebase-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "firebase-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
